# Copy to .streamlit/secrets.toml (gitignored). DATABASE_URL env var
# takes precedence, so local Docker dev needs no secrets file at all.
#
# Local Docker:
# database_url = "postgresql://elderly:elderly_dev@localhost:5432/elderly_test"
#
# Streamlit Cloud: paste Neon's POOLED connection string
# (host contains "-pooler").
#
# Step 3 debug gating (absent = dev tools stripped; URL param inert):
# dev_tools = false