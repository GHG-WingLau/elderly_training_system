streamlit>=1.62.0
jsonschema==4.23.0
pytest==8.3.3
psycopg[binary]==3.3.5
# Minimum validated runtime: Streamlit 1.28 (enforced by tests/
# test_environment.py, which also guards the psycopg driver).
# The pins above are the release target — run the full suite on it before
# sign-off.
# edge-tts is INTENTIONALLY ABSENT: dev-only tooling (tools/generate_audio.py),
# never imported by the app runtime.