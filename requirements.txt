streamlit==1.62.0
jsonschema==4.23.0
pytest==8.3.3
psycopg[binary]==3.3.5
# EXACT pins: the deploy log showed Cloud resolving streamlit>=1.62.0 to
# 1.63.0 — an unvalidated runtime outside every platform fact recorded in
# DECISIONS.md (iframe sandbox model, st.iframe positional handling,
# st.audio behavior). tests/test_environment.py guards the exact pin.
# Minimum validated Streamlit remains 1.28 (floor test retained).
# edge-tts is INTENTIONALLY ABSENT: dev-only tooling
# (tools/generate_audio.py), never imported by the app runtime.