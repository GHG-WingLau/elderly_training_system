Decision log. Rows are never deleted — superseded rows are retained andmarked. Columns: version · decision · location · rationale.

Consolidated twice: Sept 2026 (post-#7/#8, 204 tests) and again at 232tests green, covering changes #1–#14, narration, and platform findings.Phase 1–5 rows reconstructed from the v4.1 reference pack — verify viagit diff; Phase 6 rows consolidated from per-change appends.

Phase 1 — Onboarding & assessment
| v4.1 | Red-flag fast-path skips SARC-F/baseline/preference; level locked Level 0 | onboarding_wizard step 0 | Safety; data collection deferred to supervised intake || v4.1 | UPST uses best of left/right for scoring | baseline_timers | Single sub-score needed || v4.1 | Preference default = "Chair-Assisted" (safest); confirmation screen shows final level before commit | onboarding_wizard step 3 | User sees the effect before confirming || v4.1 | compute_final_level extracted as pure function (no Streamlit) | onboarding_wizard | Stable, hermetic safety contract || v4.1 | Baseline entry is manual (no live-ticking timer) | baseline_timers | Streamlit rerun model; live timer deferred |

Phase 2 — Curriculum engine
| v4.1 | Weekly pool = 2-per-category × 3 categories (constrained specialization of spec's "random") | exercise_logic | Guarantees daily 3-distinct-category + no-consecutive-repeat selection is always feasible || v4.1 | W1 = complement of W0 within {C,PC,G} | exercise_logic | Honors "remaining unused from initial pool" || v4.1 | Daily selection = alternating pattern per category (odd/even days) | exercise_logic | No exercise on consecutive days by construction || v4.1 | RNG seeded by sha256(email|cycle|week|salt); weekly pool persisted | exercise_logic | Deterministic across reruns || v4.1 | Prescriptions stored in data/prescriptions.json (data, not code) | exercise_logic | Tunable without code change |

Phase 3 — Daily loop
| v4.1 | Position = earliest incomplete day (resume, no auto-skip) | session_logic | OQ-07 || v4.1 | RPE stored as "C01:2,PC01:3" string in training_progress | session_logic | Spec format || v4.1 | Breathing metronome = pure CSS @keyframes (no JS/deps) | breathing_view | Avoids streamlit-autorefresh || v4.1 | Day 6 completion → Day 7 rest | session_logic | Phase 4 implements rest_view || v4.1 | RPE widget keys position-tagged to prevent cross-day value leak | training_view | Streamlit key persistence quirk || v4.1 | RPE selectbox int 1–5 via format_func (SUPERSEDED by Phase 5 raw-options row) | training_view, session_logic | Bugfix: session_state stores VALUE, not index |

Phase 4 — Autoregulation & rest
| v4.1 | prescriptions.json restructured numeric; display built in code | prescriptions.json | Arithmetic auto-regulation needs numbers || v4.1 | hold_s Level 0 only (10s); L1–L4 isometric cards use reps | prescriptions.json | Invents no clinical values || v4.1 | "Reduce sets OR reps −20%": reps −20% primary (floor 4); sets −1 fallback (floor 1) | autoregulation_logic | Deterministic choice of the spec's "or" || v4.1 | "Maintain" = cumulative fold with caps (reps 4–20, hold 5–20s, sets ≥1); no reset on regression | autoregulation_logic | Caps bound compounding || v4.1 | Week 0 average excluded from volume fold but counts toward regression streak | autoregulation_logic | Safety; week 0 has real RPE data || v4.1 | Regression at Day 7 with mandatory acknowledgment; floor Level 0 | rest_view | Elderly-safe, auditable || v4.1 | Day 7 of Week 4 → PROGRAM_COMPLETE | state_machine | Spec §3.4 || v4.1 | Cycle restart: advance_cycle on either action; re-assessment latest-wins; history preserved via cycle-keyed tables | program_complete | OQ-06 || v4.1 | user session dict refreshed from DB after any level/cycle change | STATE.md | Prevents stale cached level |

Phase 5 — E2E hardening & polish
| v4.1 | Debug override: session-only, validated, affects reads AND writes; cleared when ?debug absent | dev_controls, session_logic | QA traversal || v4.1 | RPE selectbox: raw emoji options, distinct per-exercise labels; normalize_rpe_score parses (supersedes Phase 3 row) | training_view | AppTest selectability + screen-reader labels || v4.1 | Sex selectbox: raw = display labels | auth_view | Same rationale || v4.1 | CSS/asset paths absolutized from file | app.py, training_view | CWD-independence || v4.1 | Light theme default via config.toml; dark via Settings menu | config.toml | CSS is size/contrast-only || v4.1 | E2E DB verified via raw sqlite3 reads; helpers_e2e label-iteration lookup | tests | Version-agnostic assertions || v4.1 | compute_current_position body restored after a bad "insert above" instruction | session_logic.py | Process fix: shared modules always get full-file replacements || v4.1 | AMENDED RULE: explicit keys on form INPUT widgets only; form_submit_button takes NO key | auth_view, baseline_timers, rest_view | Corrected crash-looping overreach || v4.1 | Minimum validated Streamlit 1.28; pin now 1.62.0 (Errata E-1); test_environment.py guards | requirements, tests | Unpinned anaconda Streamlit caused E2E failures || v4.1 | E2E STRATEGY: AppTest does not UI-drive the onboarding wizard; wizard covered hermetically | tests | AppTest st.form/rerun replay limitation || v4.1 | Plain st.button widgets get explicit keys | views | Consistent keyed-widget profile || v4.1 | Workout E2E stops at WORKOUT_SUMMARY; day-advance via DB + unit tests | tests | Replay risk vs coverage |

Phase 6 — Post-MVP pass, changes #1–#14 (consolidated Sept 2026, 232 green)
Change dispositions
| v4.1 | #1 theme toggle — dropped; dark mode remains via built-in Settings | — | Night mode judged not useful for the audience || v4.1 | #2 mobile scroll/dropdown — closed as Brave-simulator artifact (Chrome sim + real S23 clean); Brave sim unsupported as QA env | QA protocol | custom.css audit found nothing scroll-arresting || v4.1 | #3 login persistence — parked (revisit at auth hardening); returning users already land on the hub; logout control identified as prerequisite, unbuilt | — | Testing-phase credentials + health-adjacent data; no supported persistence mechanism || v4.1 | #4 breathing illustrations — "image" field per practice in breathing_sequences.json (DB01–05.jpeg), rendered via shared utils/assets.render_image; additions-only JSON edit; SUPERSEDED an interim convention-mapping proposal made while the JSON was unavailable | breathing_view, tests/test_assets.py | Data-carried mapping mirrors exercise_cards.json || v4.1 | #5 approved clinical copy — "purpose"+approved "instructions" on 24 cards, "purpose"+"instruction" on 5 practices, verbatim from the team-approved document; DRAFT purged; purpose renders after title BEFORE the visual, instruction after cadence/position as plain st.write (italics dropped); titles standardized to "(Chair)" (C03/G02 — repo canonical, doc variants don't propagate; renames must update RPE-label test selectors) | data/.json, views, tests/test_copy_contract.py | Signed-off source; "description before presentation" || v4.1 | #6 metronome start/stop — per-practice Start/Stop; ring only after Start (breathing_started, STATE.md); mutual exclusion; auto-stop via animation-iteration-count = cycles + fill-mode forwards; Stop unmounts, Start restarts; ring aria-hidden; labels (user-approved B+D): "▶/⏹ Start/Stop the breathing circle — {title}", code prefix dropped (subheader keeps DBxx); guidance copy names the same object; animation runs UNCONDITIONALLY (product decision — reduced-motion override disabled in-source; static cadence text is the fallback; Start/Stop satisfy WCAG 2.2.2); geometry: margin 40px auto + RING_HEIGHT 240 (scale overflow above the iframe origin is unrecoverable by height) | breathing_view, tests/test_breathing_controls.py | WCAG 2.2.2; exact prescribed cycles; pure-CSS stance preserved || v4.1 | #7 baseline entry steps — 0.1→0.5 for calf + both SLS; chair stand unchanged; scoring untouched (0.5-grid band reachability verified; free-typed off-grid accepted on 1.62) | baseline_timers (sign-off recorded) | 60+ motor precision; non-clinical || v4.1 | #8 baseline illustrations — four ASSESS_.jpeg above inputs, visible by default, via utils/assets; captions neutral; page DRAFT caption retained pending clinical review of image content | baseline_timers, tests/test_assets.py (sign-off recorded) | Comprehension || v4.1 | #9 scroll reset — fingerprint (authenticated, current_state, onboarding_step) after each completed render; on change emit instant scrollTo(0,0) via st.iframe, height=1; plain reruns preserve viewport; login included; debug_override excluded; emission failure swallowed (never crash a render); regression report (SARC-F→Baseline) closed NOT-REPRODUCED post-restart with console proof of firing + stMain movement; contingency if it recurs: emit at top of main() | app.py, tests/test_scroll_reset.py | Stale offsets made new pages look truncated || v4.1 | #10 rest-day progression chart — parked (next feature phase); design notes: st.line_chart builtin, source rest_assessments; open: zero-value semantics, trend-copy clinical review | — | Dedicated feature || v4.1 | #11 in-app feedback form — parked | — | Superseded by #11-B contact channel || v4.1 | #11-B support contact — v1 plain mailto footer → v2 anti-harvest (SVG image + split parts) → v3 sandbox-safe + mobile fit (final): address shown only as base64 SVG data-URI; mailto assembled at load onto a real anchor (served source holds dot-split parts only; assembled address exists only in runtime DOM — accepted residual: script-executing harvesters); click navigates the iframe ITSELF; short lead line + responsive SVG + overflow:hidden; 48px/18px inline; WCAG 1.4.5 deviation recorded with rationale; rendered centrally from app.py on every page incl. login; email-only (Sheepandfish.fit@gmail.com, subject "Enquiry and Comment"); parent .support-contact CSS rules removed (parent CSS can't reach iframe content) | utils/contact.py, app.py, tests/test_support_contact.py | Locked-out users have no other channel; v2 defects: SecurityError + mobile scrollbars || v4.1 | #12 narration — instruction-only, verbatim, alongside text (never instead); "audio" field (assets/audio/{code}.mp3) on all 29 entries; B1 player: native st.audio always visible, width="stretch", NO autoplay, NO session flags; CSS audio{height:48px} lifts the measured 382×40 control; generator tools/generate_audio.py (edge-tts, dev-only dependency, NOT in requirements; en-US-AriaNeural at −15% LOCKED as the narration standard; 4 concurrent; reads text from the JSONs — verbatim by construction); manifest.json binds each mp3 to sha256 of source text — drift guard enforced in pytest: any copy change must regenerate affected mp3s in the same change; narration spot-verified verbatim | views, utils/assets.py, tools, tests/test_audio_contract.py | Prototype findings on 1.62 (see platform facts) || v4.1 | #13 baseline approved copy — descriptions + patient instructions from data/baseline_instructions.json (data, not code; cache precedent); placement follows #5 pattern; shared SLS description rendered once (approved right-side instruction itself states the repeat — no copy invented); reads fail-loud; page DRAFT caption retained pending #8 illustration review; FLAGS for team: "endurances" verbatim-typo; clinician-voice instructions assume assisted administration (gates future narration); 31 cm figure is display-only; "45/60 s" vs 120 s input cap | baseline_timers (sign-off recorded), tests | Approved copy in hand || v4.1 | #14 label readability — Step 0 guidance "Check all boxes that apply to you. If none apply, leave them unchecked and press Continue." (states existing behavior); baseline labels verb-first ("Enter your calf circumference (cm)", "Enter your single-leg stance time — left/right leg (sec)", "Enter your 30-second chair stand count (reps)"); LABEL_* constant names unchanged; values pinned by test (future renames fail loudly); verified: 232 green, zero text-source label matches | onboarding_wizard, baseline_timers (sign-off recorded), tests | Labels were test names, not instructions |

Platform facts (pinned Streamlit 1.62.0)
| v4.1 | st.iframe is the single validated raw-HTML vehicle: positional argument accepts raw HTML (heuristic — parameter is NAMED src yet takes HTML; validated for script-bearing AND script-free fragments); signature (src, width, height, tab_index); "srcdoc=" is NOT a parameter — barred from current and future changes (its window was masked by the emission try/except: scroll resets silently no-op'd with pytest green) | app.py, breathing_view, utils/contact.py | Multiple prototype cycles; record must match shipped code || v4.1 | st.iframe sandbox model (complete): allow-scripts + allow-same-origin, NO allow-top-navigation — parent DOM access works (scroll reset), top-window navigation throws SecurityError (contact v2 defect), navigation is only ever frame-self; Chrome logs "can escape its sandboxing" once per iframe (2–3/page) — Streamlit platform configuration, not triggerable or changeable, first-party code only → known-benign console noise, standing QA expectation | all iframe fragments | Two DevTools messages across QA cycles form one model || v4.1 | st.audio on 1.62: autoplay + width parameters exist; unmounting does NOT stop playback; autoplay fired only on the FIRST mounted player (second tap: no start, no overlap, first kept playing) — autoplay is unreliable; B1 chosen accordingly; native control measures 382×40 | breathing/training views | Prototype measurements || v4.1 | Other: st.html not usable on 1.62; st.components.v1.html deprecated upstream (notice after 2026-06-01), not shipped; st.number_input accepts free-typed off-step values; rendering of images/audio invisible to AppTest — completeness is enforced by static/contract tests, behavior by manual QA | — | Version-pinned facts for future work |

Process & test-suite rules
| v4.1 | Full-file replacements only (no "modify in place"); per-change pytest runs; label renames update matching test selectors in the same change (constraint 3) | all | House discipline || v4.1 | Module-level JSON caches (_bs_cache, _cards_cache, _bi_cache) are process-lifetime — dev-server restart required after data edits; prototypes are throwaway, standalone, deleted after | dev process | Cache lessons || v4.1 | Contract tests over generated HTML/CSS: target CODE not comments (strip /* / and — note the metronome's in-source disabled override — never assert on commented blocks); DECODE embedded media (base64 data-URIs) before asserting attributes; ban-lists cover the full surface (window.top/parent, top/parent.location) | tests | Two test-defect incidents (223→221 era) || v4.1 | Test-count reconciliation discipline: a prediction/actual mismatch triggers a recount — it caught a silently dropped contract test (test_metronome_fragment_is_script_free, restored) | tests | 226 vs 227 || v4.1 | Repo contract greps use --include=".py" (or -I): bytecode matches are noise; orphaned pycache .pyc from retired modules is inert archaeology (test_onboarding_e2e.pyc vs the live test_e2e_onboarding.py); rm -rf tests/pycache is always safe | QA protocol | #14 verification || v4.1 | Permanent manual-QA lines: (a) transitions snap to top + one [scroll-reset] console line; (b) metronome renders/animates and auto-stops at prescribed cycles; (c) real-device checks (simulators insufficient — Brave artifact lesson); (d) device-matrix pass 320/375/414px; (e) contact-footer tap opens mail app on a real device; (f) spot-listen narration when audio is generated/changed; (g) sandbox warning is expected console noise — only NEW messages and [scroll-reset] matter | QA protocol | AppTest blind spots |

| v4.1 | Deployment gates decided: psycopg[binary] = the one approved runtime dependency; stdlib password auth incl. parked #3 remember-me (un-parked); debug STRIPPED in prod (URL param honored only with server-side dev-tools config; inert on Cloud); test strategy A with local Docker Postgres + Neon pre-deploy; consent copy received (v1) — data/consent.json at Step 2, consent_version + accepted_at recorded at registration; GitHub repo public with the cleartext-address risk accepted for now | — | Pre-deploy decision set |
| v4.1 | Step 1 DB port (stage 1): db/database.py + queries.py ported to psycopg (%s placeholders, dict_row, native FKs, statement-split schema init; URL from DATABASE_URL env then st.secrets; Neon pooled string guidance); NEW schema_postgres.sql in FINAL shape (IDENTITY PKs; password_hash/consent_*/sessions inert until Step 2 — one schema authoring, no second migration); db/testing.py reset_database(); docker-compose.yml (postgres:16, elderly_test); .streamlit/secrets.toml.example; SQLite retired from runtime (schema_sqlite.sql kept for history); ON CONFLICT idempotency carried verbatim. Stage 2 (paste-driven): conftest/helpers/E2E ports + requirements.txt/test_environment pin updates | db/*, docker-compose.yml | Strategy A; the doc's "Neon router" note discharged |
| v4.1 | Consent copy v1 received — flags for the team: (a) two differing checkbox lines in the document (full-doc vs mobile-UI) — mobile-UI line used on screen per the document's designation, confirm intentional; (b) micro-copy references "tapping 'I Agree'" but the registration form's agreement action is the checkbox — accept mismatch or issue v1.1 | data/consent.json (Step 2) | Verbatim discipline: signed copy is never silently reworded |

| v4.1 | Step 1 stage 2 (suite on Postgres — strategy A closed): conftest sets DATABASE_URL before any test import (defaults to the docker-compose local URL — pytest needs no export) + REMOTE-HOST GUARD + autouse per-test TRUNCATE (replaces per-test SQLite files; DB_PATH monkeypatch retired); helpers_e2e.db() = fresh psycopg dict_row/autocommit read connection; make_app signature preserved, DB-path patching removed; both E2E files ported (%s, dict rows — counts unchanged); test_environment gains psycopg>=3.1 guard; requirements.txt gains psycopg[binary]>=3.2,<4.0 (edge-tts stays dev-only, absent) | tests/*, requirements.txt | Single dialect end-to-end: app, tests, Docker, future Neon |
| v4.1 | Safety design (truncate guard): a DATABASE_URL pointing at Neon run through pytest would TRUNCATE every table — the guard converts that from a data-loss event into a collection-time refusal unless TEST_DB_CONFIRM_REMOTE=1 is set deliberately | tests/conftest.py | Shared-DB truncation footgun identified during stage-2 design |
| v4.1 | Consent copy v1.1 registered (v1 flags resolved by team): lead "By checking the checkbox, you confirm that:"; harmonized checkbox "I am 18+ and agree to the Health Waiver, Liability Release, and Privacy Terms."; ships as data/consent.json with Step 2 (registration gate + consent_version/consent_accepted_at recording) | data/consent.json (Step 2) | v1 flags: divergent lines + "tapping I Agree" |

| v4.1 | Step 1 stage-2 scope miss, diagnosed via count discipline: predicted 233 / collected 233, with 29 setup ERRORS confined to three logic-test files (test_autoregulation_logic 15, test_exercise_logic 7, test_session_logic 7) whose private iso_db fixture swaps queries.get_connection to a raw SQLite connection — incompatible with the psycopg %s dialect. All tests that ran under Postgres passed incl. all 5 E2E — the application port is verified; the residue is test infrastructure. Root cause: the pre-port paste census enumerated 5 files but not the 3 logic files (their fixtures never pasted). PROCESS RULE: a dialect port begins with a repo-side census — grep -rln "sqlite3\|get_connection\|iso_db" tests/ --include="*.py" — and every hit is pasted before porting | tests/* | 204+29=233 exactly; paste census prevents recurrence |

| v4.1 | Step 1 CLOSED: 233 green on Postgres (strategy A complete); post-port grep of tests/ returns nil — no sqlite3/get_connection/iso_db references remain (the one earlier census hit on helpers_e2e.py was case-insensitive tooling matching "SQLite" in the docstring; current lowercase grep is the authoritative clean result) | db/, tests/ | Port complete and provably so |
| v4.1 | Step 2a password auth: PBKDF2-HMAC-SHA256 600k iterations (OWASP 2023), 16-byte salt, self-describing stored format pbkdf2$sha256$iters$salt$hash (future parameter changes verify old hashes); constant-time verification; malformed stored hash → False (login path never raises); NIST 800-63B policy — min 8, no composition rules; uniform login failure message (no user enumeration); get_user_by_credentials RETIRED; legacy no-password accounts rejected (dev DBs throwaway; beta fresh) | utils/auth.py, auth_view, queries | Gate-2 design |
| v4.1 | Step 2a security fix (found during design): create_user's UPSERT + password auth = account-takeover class — re-registering an existing email would overwrite username/age/sex while keeping the OLD password; fixed by a duplicate-email gate in auth_view BEFORE create_user ("already exists — please log in"); ON CONFLICT update set deliberately excludes password/consent columns; E2E test pins the gate (no second row, original password still logs in) | auth_view, queries, tests | Upsert semantics re-examined under auth hardening |
| v4.1 | Step 2a consent: data/consent.json v1.1 (copy is data, not code) — micro-copy + full text + harmonized checkbox "I am 18+ and agree to the Health Waiver, Liability Release, and Privacy Terms."; registration requires the checkbox; consent_version + consent_accepted_at recorded at creation (evidence columns); full text in expander + download button; the v1→v1.1 full-text agreement-line substitution is the only edit, per team harmonization | data/consent.json, auth_view, tests | Signed-off copy; un-evidenced consent = no consent |
| v4.1 | Step 2a logout (parked #3 prerequisite built): daily_hub "Log out" (btn_logout, below a divider) → utils.auth.clear_session_state (single logout path; 2b adds token deletion there); hub placement = the session's home base, every flow returns to it; other pages get logout when a top-bar pattern exists (future option, not pushed) | daily_hub, utils/auth, STATE.md | Discoverable end-of-session control |
| v4.1 | Step 2a session tokens delivered mechanism-independent: create_session_token (32-byte urlsafe, 30-day default, opportunistic purge), get_session_user (expiry-checked JOIN), delete_session_token, purge_expired_sessions — consumed by 2b under EITHER the localStorage or query-param design; pinned by tests | db/queries.py, tests/test_auth.py | 2b becomes view/boot-layer only |
| v4.1 | Step 2a.1 hotfix: st.download_button's MIME parameter is mime on pinned 1.62 — the delivered 2a used mime_type, raising TypeError and crashing EVERY unauthenticated render at auth_view:86; cascade identified from the failure pattern (fingerprint never stored → 3 scroll-test failures; registration widgets never created → 2 registration failures; login/workout E2E unaffected because the login handler's st.rerun() precedes the crash point). One-line fix (mime_type → mime). LESSON: kwargs are API surface on a pinned version — unverified parameter names are hallucination risk; the AppTest run is the cheap, reliable guard (this one died in pytest, first run, exactly as designed) | auth_view, tests | 5 failures, 1 root cause, 1 line |

| v4.1 | Step 2a CLOSED: 244 green + full QA (registration/consent evidence columns, duplicate gate, uniform login failure, logout round-trip) | auth_view, utils/auth, queries | Verified |
| v4.1 | Step 2b mechanism decided by prototype: P1 parent-localStorage write ✓, P2 replaceState URL rewrite ✓, P3 parent-rerun via synthetic button click ✗ — a fragment can store the token but can never hand it to Python at boot, and parent location.reload() is sandbox-blocked (navigation class). Mechanism (b) (localStorage) DEAD on 1.62; mechanism (a) adopted: pure-Python query-param token (?t=), zero iframe, zero unverified API surface. Prototype deleted after verdict | utils/auth, app.py, auth_view | P1/P2/P3 record |
| v4.1 | Session-lifecycle fact (user question, recorded): st.session_state lives with the browser WebSocket session — full page reload or close-reopen starts a NEW session with blank state (why a logged-in reload landed on the login page pre-2b); internal reruns keep state. This is the documented Streamlit model, confirmed on 1.62 — and is the exact gap change #3 originally targeted; try_restore_session() now closes it | STATE.md | Explains observed behavior |
| v4.1 | Step 2b trade-offs (accepted): token rides the URL — visible in address bar and browser history; bookmarks carry a live 30-day session (a feature for this audience); a shared link grants access until expiry or logout; logout is the invalidation path (single path: clear_session_state deletes row + removes param); dead tokens are scrubbed from the URL at boot; multiple concurrent tokens per user supported (two devices) | utils/auth | Beta-acceptable; revisit if threat model changes |

| v4.1 | 2b.1 fix (one-line, my defect, caught by per-change pytest): Postgres has no max(boolean) — SQLite coerces MAX(bool) numerically, the SQLite-ism lived in the NEW test SQL, not ported code. LESSON: assertion SQL is code — dialect rules apply; prefer filter+COUNT over boolean aggregation; second consecutive one-line defect in newest-written lines (mime_type kwarg, then this) — the port and ported suite remain defect-free across all three runs | tests/test_e2e_onboarding.py | 248 passed + 1 failed → 249 expected |
| v4.1 | Remember-me persistence boundary (expected behavior, recorded): the token rides the URL only — reload/bookmark/home-screen icon/ctrl-shift-T/history-with-?t all restore to the hub; a fresh navigation to the bare URL carries no token → login page. Mitigation for beta: bookmark-or-home-screen guidance at first login. Gap is structurally unfixable without browser storage; the one untested channel is Streamlit auto-rerun on replaceState (P4, offered, not built) — decision: accept for beta / prototype P4 | auth design record | User question 2026-09-07; mechanism (a) boundary |

| v4.1 | Step 2b CLOSED: 249 green after the 2b.1 dialect fix; reload → hub verified; close-reopen boundary ACCEPTED for beta (bare-URL navigation carries no token — structurally unfixable without the P3-blocked storage channel; P4 auto-rerun probe declined); prototype deleted | — | Verified + accepted boundary |
| v4.1 | Bookmark reminder (user-approved): hub st.info tip shown ONLY when the remember token is in the URL (?t=) — targeted at exactly the sessions where bookmarking captures persistent login; declined-remember users see no tip (advice wouldn't apply); always-on-while-token-present for beta, dismiss-once variant recorded as post-beta refinement (needs DB state); wording covers desktop bookmark + phone Add to Home Screen; caveat recorded: bookmark made before a later logout holds a dead token — one re-login + re-bookmark recovers | daily_hub, tests/test_bookmark_tip.py | Persistent-login guidance for the elderly audience |
| v4.1 | Step 3 debug gate: ?debug=true honored only with server-side dev_tools secret (st.secrets, default-deny when no secrets file); on Cloud the secret is absent → parameter fully inert, controls stripped in production (user decision "stripped"); local dev opts in via .streamlit/secrets.toml (documented in secrets.toml.example); hermetic tests monkeypatch st.secrets | app.py, utils/dev_gate.py, tests/test_dev_gate.py | Production hygiene |
| v4.1 | docs/DEPLOYMENT.md runbook delivered: Neon main=production / beta-test=disposable branch for the pre-deploy suite (TEST_DB_CONFIRM_REMOTE only ever against a disposable branch), Cloud settings (app.py main, Python 3.12, database_url secret, dev_tools deliberately absent), deployed-URL smoke = the permanent QA lines, beta ops (invite list, daily inbox check, Neon-console review incl. consent audit + RPE-adverse scan), rollback (redeploy / Neon restore), known beta limitations recorded | docs/DEPLOYMENT.md | Closing pre-deploy artifact |

| v4.1 | Deploy hotfix — Neon idle-drop root cause + self-healing connection: Neon free tier suspends idle computes (~5 min) and the pooled endpoint drops idle clients; the cached @st.cache_resource connection dies between user actions — deployed symptom was OperationalError "SSL connection has been closed unexpectedly" on the first write after an idle gap, and without healing the dead connection stays cached, failing ALL subsequent actions until process recycle. Fix: get_connection() returns _HealingConnection (duck-typed proxy) — OperationalError on execute → close + reconnect (schema re-applied idempotently) + one retry; safe because every write is a single-statement UPSERT (Phase-1 idempotency pays off) and every read is stateless; commit failure reconnects then re-raises (rare execute→commit window; user retry re-applies via UPSERT); rollback heals silently (protects reset_database); prepare_threshold=None (pooler portability, defensive); unit tests drive the proxy with scripted fakes | db/database.py, tests/test_connection_healing.py | Boot worked (secrets + pooled string correct); ~8-min form-fill gap spanned the suspend window |
| v4.1 | Deploy discovery — version drift: Cloud resolved "streamlit>=1.62.0" to 1.63.0 (floor, not pin) — the deployed app ran an unvalidated runtime outside every recorded platform fact (iframe sandbox, st.iframe positional handling, st.audio autoplay, mime parameter). requirements.txt now EXACTLY pins streamlit==1.62.0, guarded by test_streamlit_pin_is_exact (parses requirements.txt — the floor test remains the runtime check). Other install facts: psycopg 3.3.5 as pinned ✓; secrets wiring confirmed working (schema applied at boot) ✓; Cloud's automatic pyarrow 25→24 downgrade is platform behavior, no action | requirements.txt, tests/test_environment.py | "Pinned in requirements.txt" claim vs floor reality — repo wins, file now matches the claim |

| v4.1 | Healing-harness v2 (4 v1 failures were TEST-HARNESS defects; the proxy itself is vindicated by the tracebacks — it caught, reconnected, and retried, and the retry failed only because the scripted fake's script was exhausted): (a) _healing seeded the reconnect queue WITH the initial connection, so every _reconnect re-served the dead conn → IndexError; queue is now conns[1:], unscripted reconnects fail with a clear assertion; (b) the commit test asserted a post-heal return from an empty script — post-heal actions are scripted explicitly. RULE: script-driven fakes must script every action the code under test will take, including post-heal ones | tests/test_connection_healing.py | db/database.py unchanged — implementation verified by its own failure traces |
| v4.1 | Remote-run milestone: the full suite has now run against a REMOTE Neon database — 254/258 passing (the 4 failures DB-independent harness bugs), including all E2E; DEPLOYMENT.md §1.3 (branch validation) effectively complete pending the fixed rerun; the remote-host guard + TEST_DB_CONFIRM_REMOTE operated as designed. Suite now validated on BOTH local Docker and remote Neon | tests, QA record | Strategy A proven end-to-end on the production database platform |
| v4.1 | Defect-location pattern (third instance, now a named rule): every failure across the last three change-runs lived in NEWLY WRITTEN code — never in ported code, never in code that had survived a prior green run (mime_type kwarg → max(boolean) SQL → fake-harness queue seeding). The collected-count arithmetic scoped each failure set exactly (254+4=258). Consequence for process: newest-written lines get the most skeptical review, and count reconciliation stays mandatory | process record | Pattern recognition across 2a.1, 2b.1, healing-v1 |

| v4.1 | Healing-harness v3 (1 v2 failure, four-character defect): _RollbackRaisingConn lost its no-arg constructor when moved above _healing in the v2 rewrite — _FakeConn requires 'script'; restored (defaults to empty). Fourth consecutive incident in the same class: defect in NEWLY WRITTEN test code, healing/application code innocent in all of them. RULE (extended): moving helper classes re-verifies every call site's constructor expectations; the count arithmetic again scoped it exactly (257+1=258) | tests/test_connection_healing.py | Named-pattern discipline pays |

| v4.1 | Deploy hotfix round 2 — idle-in-transaction (drill 2026-09-07, root cause app-side): with autocommit=False every SELECT opens an implicit transaction and NO read function committed/rolled back — the cached connection idled IN an open read transaction between user actions; Neon's idle_in_transaction_session_timeout (~5 min) terminated it server-side (25P03). Local Docker Postgres has the timeout DISABLED — the suite could never catch it; remote-branch validation (§1.3) is the environment class that would. Two-layer fix: (1) PREVENTION — every read in queries.py ends with conn.rollback() (connection idles OUT of transaction; 25P03 unreachable in normal operation); (2) DETECTION — healing catch set is the DEAD_CONNECTION_ERRORS tuple; psycopg 3.3.5 does NOT map 25P03 under OperationalError (empirical: the drill's exception escaped the round-1 handler — traceback line 92, not the retry at 95). Hygiene pinned by pg_stat_activity tests (state must be 'idle', never 'idle in transaction') | db/queries.py, db/database.py, tests | Round-1 healing was live and structurally working; the failure was a narrow catch set + an unaddressed connection state |
| v4.1 | Neon connection-lifecycle model (two distinct killers, both now handled): (a) compute SUSPEND / pooled-endpoint idle drop → SSL-closed errors → OperationalError → healed since round 1; (b) idle-in-TRANSACTION termination → 25P03 → NOT OperationalError in psycopg 3.3.5 → tuple-extended in round 2. Prevention (b) via read-rollback; healing covers both. Platform fact for any future DB-layer work | db/database.py, QA record | Two incidents, one model |
| v4.1 | Finding B CLOSED: deploy log shows - streamlit==1.63.0 + streamlit==1.62.0 at the 06:26 update — the exact pin is live on Cloud | requirements, deploy record | Confirmed via boot log |
| v4.1 | Permanent QA line (extends the deploy checklist): the IDLE-GAP DRILL — leave the app idle 6+ minutes on the login page AND while logged in, then act; both must succeed transparently. The drill is the only reliable reproducer of Neon lifecycle kills (local Docker cannot reproduce them) | QA protocol | This incident was found by it; it stays in the protocol |

| v4.1 | Hygiene-harness v2 (both v1 failures were observer-contamination — the probe raced/perturbed the transient pg_stat_activity state, asserting 'idle' against a tri-state oracle; same defect class as healing-harness v1): redesign — oracle is the FORBIDDEN state ('idle in transaction' never observed during a 2s polling window on the probe's own autocommit connection); a genuinely stuck transaction holds the forbidden state for the whole window (impossible to miss); PLUS a negative-control test (deliberately-unreleased transaction must be detected) — the harness now proves it can catch the incident it exists to catch. RULE (3rd instance of the class): observation of state must not perturb it; transient tri-state oracles are asserted by absence over a window, not equality at a snapshot; regression harnesses carry a self-test | tests/test_transaction_hygiene.py | 260+2=262 collected; both failures deterministic probe artifacts |

| v4.1 | Deploy hotfix round 2 CLOSED: 263 green (260 + 3 — hygiene v2 grew by one, the negative-control test; the prior "+2" prediction note miscounted, corrected), BOTH idle-gap drill variants passed on the live app (login-page idle → login; logged-in hub idle → Start) — 25P03 incident formally closed. Neon lifecycle fully handled: suspend + idle-drop (healed since round 1), idle-in-transaction (prevented + healed since round 2) | — | Incident record; drills are permanent QA lines |
| v4.1 | Production cutover (recommended pattern): existing Cloud app repointed from Neon beta-test to MAIN via secret swap (database_url → main-branch pooled string) — no new app needed; beta-test test data stays quarantined on the disposable branch, main starts clean; schema auto-applies on first boot; beta-test branch retained for pre-deploy suite validation; dev_tools stays absent from production secrets; optional separate staging app noted, declined for supervised beta | Cloud secrets, Neon | Beta launch |
| v4.1 | Localization handoff delivered: docs/LOCALIZATION_HANDOFF.md — self-contained context pack for the next-phase chat (binding rules with localization extensions of constraints 2 and 3; 1.62 platform-facts compendium; Neon/DB facts; data-file localization inventory incl. never-localize columns; architecture options; 10 pre-coding decisions; sequencing; paste checklist; carried parked items) | docs/LOCALIZATION_HANDOFF.md | Phase transition artifact |

Phase 7 — Localization (zh-HK Cantonese + Traditional-Chinese Mandarin zh-TW), consolidated Sept 2026 at 312 tests green
Pre-coding decisions (7.1–7.5, team)| v4.1 | Locales fixed: {"en", "zh-HK", "zh-TW"}; zh-TW per Taiwanese written-Chinese conventions | utils/locale.py | HK/TW vocabulary diverges despite shared script || v4.1 | Data shape: per-locale files; EN keeps base filenames (consent.json / consent.zh-HK.json …) | data/ | One locale per diff; loader mirrors the cache pattern || v4.1 | Locale selection: ?lang= query-param persistence, "en" fallback; picker on auth page + hub; post-auth persistence to users.locale; URL overrides profile and never writes it; the default is never written to the session | utils/locale.py, app.py, auth_view, daily_hub, db | st.query_params = the only Python-readable client channel on 1.62 (?t= precedent); bookmarks carry the choice || v4.1 | Tests: default-locale (en) runs keep every existing selector green + per-locale completeness tests (every key exists, no EN leakage, no DRAFT) + per-locale E2E smoke | tests | Cheapest green path; label contracts go data-driven where they localize || v4.1 | Translated clinical/legal copy = NEW clinical content: entered verbatim from team-approved clean-text sources; corrections applied only on team answers and pinned by tests | data/*.json, tests | Verbatim discipline — signed copy never silently reworded |

Data layer| v4.1 | users.locale (nullable TEXT) — first post-"final-shape" schema amendment: CREATE + idempotent ALTER TABLE … ADD COLUMN IF NOT EXISTS (lands on live MAIN at next boot; no manual migration) | db/schema_postgres.sql, db/queries.py | 7.4.3; additive, nullable, no backfill || v4.1 | Registration captures the session locale into users.locale (decision a); the picker stayed gated invisible until activation (decision b) | auth_view, create_user, utils/locale.py | Team decisions || v4.1 | All five doc classes landed per locale: consent (versions "v1.1-zh-HK"/"v1.1-zh-TW" recorded at registration — §7.8), baseline, rubrics (red_flag_labels added), breathing, exercise cards (24×2); never-localize fields pinned byte-equal to EN | data/, tests/test_locale_data.py | §5 inventory; completeness contracts || v4.1 | Threshold scoring stays on EN rubrics, structurally: utils.assessment_logic reads data/scoring_rubrics.json only; locale rubrics feed display (SARC-F item labels/options — option ORDER is the options.index scoring contract) | utils/assessment_logic, sarc_f_assessment | Never-localize enforced in code, not only by tests || v4.1 | Locale-aware loaders: per-locale caches, EN base filenames, fail-loud missing files, safe_locale() bare-mode shim | breathing_logic, sarc_f, baseline_timers, auth_view, exercise_logic | Cache precedent; process-lifetime caches || v4.1 | UI-strings layer: data/ui_strings.json (EN, 130 keys) + ui_strings.{zh-HK,zh-TW}.json + utils/strings (tr / level_display / tr_locale, fail-loud) | data/, utils/strings.py, tests/test_ui_strings.py | The consent-checkbox data-driven pattern, generalized || v4.1 | Level composition: EN levels map gains descriptors (the approved PREFERENCE labels); EN templates tokenize {n} matching the signed zh placeholders — one code path for all locales; placeholder parity machine-checked | ui_strings, exercise_logic, tests | The translator parameterized level text EN held literal |

Conversion + activation| v4.1 | View conversion: every view + locked components (team sign-off, localization thread) render via tr()/level_display; EN contract constants retained (LABEL_*, REMEMBER_LABEL, BOOKMARK_TIP, RPE_OPTIONS, SEX_OPTIONS, PREFERENCE_OPTIONS, PICKER_LABEL); widget keys/form IDs unchanged; page_title localizes from ?lang= at first load only (set_page_config applies once — documented trade-off) | app.py, views, locked/, utils | 7.5 default-locale runs untouched || v4.1 | ACTIVATION: SUPPORTED_LOCALES ships all three locales; picker live; wizard preference selectbox index-mapped to Level {i}; zh-HK E2E smoke (register→wizard + login→hub, data-driven labels, DB-verified users.locale + consent_version) | utils/locale.py, tests/test_e2e_locale.py | End-to-end activation proof || v4.1 | Audio localization: tools/generate_audio.py --lang; voices en-US-AriaNeural (locked), zh-HK-HiuMaanNeural, zh-TW-HsiaoChenNeural; rate −15% all locales to start (tune after listening — every manifest entry records voice+rate); EN top-level layout unchanged (filenames contract); per-locale manifests + drift guard (15 tests); per-locale spot-listen passed | tools/, tests/test_audio_contract.py | §8 step 5; narration verbatim by construction, per locale || v4.1 | Accessibility: picker label = FIXED bilingual wayfinding string ("Select your preferred language / 請選擇你的語言"), identical in every locale — the switcher must be findable BEFORE the user understands the UI language (cold-start problem); identical-by-design exemption set; pinned | ui_strings ×3, utils/locale.py, tests | User request; EN wording polished one word (Selecting→Select), user-approved variants recorded (🌐 / register-neutral 請選擇語言) || v4.1 | EN copy decisions: breathing cadence "(~2 min)" tail dropped (data instruction text carries durations); baseline DRAFT caption removed (#8 illustration review resolved); SUPPORT_SUBJECT not localized; DB03–05 EN identifier-style titles accepted as divergence | breathing_view, baseline_timers, contact.py, data | Team sign-off |

Process records| v4.1 | macOS clipboard apply protocol, born from four truncation incidents (auth_view:92, breathing_view:145, training_view:111, ui_strings.json:147): one file at a time; pbpaste > path (never terminal-editor paste); code-block copy button; tools/preflight.sh (compileall + JSON parse of all data files) after every apply, before pytest; tail markers on every delivered full file; git diff --stat; commit per green run | process record | The paste-clipping failure class converted into named single-file failures || v4.1 | Count discipline: one dropped-test catch (281 vs 289 — v2 tests delivered as "additions", the original 8 + helpers lost on apply; the full-file rule now explicitly covers TEST files); every other Phase 7 run reconciled exactly (263 → 271 → 273 → 282 → 289/291 → 300 → 302 → 311 → 312) | tests, process | The 226-vs-227 precedent repeated and closed || v4.1 | Defect-location pattern, Phase 7 count: seven assistant-delivered defects, ALL in newest-written lines, ALL caught by guards (picker widget-state desync → wiring test; prescription split one-space → pin test; test-set drop → count discipline; _tr en-path → smoke setup; picker-label selector → constraint-3 boot test) — the named rule demonstrably applies to assistant-authored code | process record | The guards behaved as designed, every time || v4.1 | Constraint-3 localization extension enforced: a localized label IS a label change — test selectors (or data-driven label reads) update in the same change; enforced once against the assistant's own activation change (the picker label) | tests/test_locale.py, process | The rule's first self-application || v4.1 | Phase 7 remaining (§8 step 6, open tail): typography pass (CJK font stack / line-height; contact-fragment fonts) + the QA matrix (locale × 320/375/414 × the permanent manual lines + idle-gap drill on production); per-locale spot-listen done | roadmap | Phase close-out tail || v4.1 | Carried parked items (do not let them evaporate): #10 rest-day progression chart; #11 in-app feedback form; P4 auto-rerun probe (declined for beta); bookmark-tip dismiss-once; public-repo cleartext-address acceptance; NEW Phase 7 parked: per-locale TTS rate tuning (after more listening); zh-TW E2E smoke mirror; typography follow-ups from the QA matrix | DECISIONS | Cross-phase carry |

| v4.1 · Copy consistency edit (team, 312 green): 復盤→回顧 (zh-HK) and 複盤→回顧 (zh-TW) across all affected ui_strings entries — extends the earlier start_review correction; UI-strings only, no data files affected, no pinned strings touched | data/ui_strings.{zh-HK,zh-TW}.json | Terminology consistency |

Phase 8
| v4.1 · Phase 8 decision (user-approved): in-flow dismissible guidance + persistent help affordance — NOT a spotlight overlay tour (P3: fragments can't drive the app; DOM selectors churn across reruns; AppTest-invisible; elderly cognitive-load evidence favors contextual over upfront) | utils/hints.py, daily_hub | Option B |
| v4.1 · users.ui_hints (JSONB, additive idempotent ALTER — the locale-column pattern): dismissed hint key → ISO date; NULL = all active; dismiss = single-statement idempotent JSONB merge; user dict refreshed after write (Phase 4 rule); no new session keys | db/, utils/hints.py | Dismiss-once + always-available help = safe pairing |
| v4.1 · Hub hint + 「How to use」expander shipped (provisional EN constants — the REMEMBER_LABEL pre-localization pattern; ui_strings untouched this change) | utils/hints.py, daily_hub, tests | Predicted 315 |
| v4.1 · PENDING next change: remaining hint points (breathing/training/summary/rest), ui_strings migration + zh-HK/zh-TW translation (~12–14 new strings through the standard pipeline), data-driven 「Got it」 selector, bookmark-tip dismiss-once un-parked onto the same store | roadmap | Step 3 |

| v4.1 · Beta feedback (live site, first-visit): landing page read as "register for a 30-day paid trial" — root causes: no orientation block; REMEMBER_LABEL's "(30 days)" on the default-active login tab read as program duration; no cost statement. Fix (user-approved direction): permanent landing orientation block (station 0 of the walkthrough — not a dismissible hint: pre-auth has no profile, and landing pages orient everyone) stating 5-week duration, 60+ audience, daily effort, free/no-cancel, and registration's actual purpose; REMEMBER_LABEL reworded to drop the duration figure (copy change through the standard pipeline, selectors auto-follow via the constant) | auth_view, ui_strings, utils/locale-adjacent copy | Trust copy for first-time elderly visitors |

| v4.1 · Phase 8 feedback fix shipped (EN-first, user-approved): landing orientation block — permanent, above both tabs, the approved four sentences (5 weeks / 60+ / 15–30 min / free, nothing to cancel / registration = progress); REMEMBER_LABEL reworded, duration figure dropped (misread as program duration; pinned); tab order kept · auth_view, ui_strings EN, tests · Predicted 318 |
| v4.1 · Hint set extended to all post-onboarding stations (breathing/training/summary/rest — provisional EN drafts, wording review before translation); zh values + ui_strings migration + data-driven Got-it selector + bookmark-tip dismiss un-park land together with the 17-string translation round · utils/hints.py, views, pipeline | One round, cross-referenced button wording |
| v4.1 · Interim state (accepted): orientation + hints render EN on zh sessions until the translation change — the hub-hint precedent · — | EN-first sequencing |

| v4.1 · Phase 8 translation round landed (17 strings × 2 locales, cross-referenced button wording; zh keeps the no-bold style): all hint/orientation copy migrated to ui_strings (146 keys, count pinned); hints render tr-driven (constants retained as EN contract values — drift pins); 「知道了」 label now localized · ui_strings ×3, utils/hints.py, views | The full hint set is live in all three locales |
| v4.1 · Bookmark-tip dismiss-once UN-PARKED onto the ui_hints store (key "bookmark_tip"; token-conditional, dismiss-once, localized body from hub.bookmark_tip) — the Phase 6 post-beta refinement, implemented · daily_hub, STATE.md, tests | Mechanism synergy, as planned |
| v4.1 · Paste-surface discipline: the three ui_strings updates shipped as a one-shot idempotent migration script instead of 3×160-line re-pastes · process record | The clipping class stays closed |

After 320 green: deploy per the standing procedure (beta-test pre-deploy suite → push → smoke), and the Phase 8 hint set is complete — orientation + five stations + help affordance + bookmark dismiss, all localized.

| v4.1 · Phase 9, Step 1 — summary debriefing shipped (EN-first, user-approved): personalized congrats; "Today you trained" body-region bullets with localized TITLES + RPE word/emoji (reuses training.rpe_N descriptors — no new copy for the ratings); DB-derived sessions-this-week counter; auto-adjust reassurance (states existing autoregulation — deliberately not manual-change advice, which would contradict prescription safety); breathing habit line; numeric average + code lines dropped by design (ui_strings keys dormant, translations retained) | summary_view, tests | Predicted 321 |
| v4.1 · Region labels = 6 new friendly strings (categories → body regions, non-scientific by design); PROVISIONAL EN constants pending the Step 1+2 translation round | summary_view | Pipeline |
| v4.1 · PENDING watch: test_e2e_workout_loop may pin the retired summary lines — same-change assertion update on failure (constraint 3) | tests | Paste with report |

After 321 green and your live EN review: send the translation request, and say the word to start Step 2 — I hold every file it touches except nothing; the paste need is only the workout E2E (requested above) and, for its schema step, nothing new either.

| v4.1 · Step 1 translation partially landed (9 of 11 strings — the return was short HABIT_NOTE ×2 and ADJUST_NOTE zh-TW; procurement count discipline flagged it; partial keys can't land asymmetrically, so those two stay provisional EN): congrats / trained-today / six region labels / sessions-week now render from ui_strings (155 keys, count pinned); summary debrief fully localized except the two notes | ui_strings ×3, summary_view v3 | Predicted 321 |
| v4.1 · The workout-E2E watch item retired (321 green — no pins on the retired summary lines); the "test_e2e_workout_loop" paste was actually test_e2e_locale.py (misgrab — the real file never arrived; not needed) | — | Record |
| v4.1 · Step 2 EN copy drafted for sign-off (why-recheck explainer — non-promissory framing accepted; baseline = stance-only v1: sit-to-stand 15s is a different test from the 30s baseline, calf not re-measured; red-flag path stores no baseline) | pending approval | Next change |

| v4.1 · Phase 9, Step 2 shipped (user-approved): users.baseline (JSONB, additive ALTER — the locale/ui_hints pattern) stores the raw onboarding measures at wizard confirmation (standard path only; red-flag fast-path stores none; re-assessment overwrites latest-wins — the Phase-4 decision mirrored); the rest page gains the why-recheck explainer (non-promissory framing) + the baseline reference (Day-1 block + per-stance-input captions); stance-only v1 — the 15s sit-to-stand is a different test from the 30s baseline (not shown), calf not re-measured | db, wizard (sign-off: storage-only), rest_view, tests, STATE.md | Predicted 324 |
| v4.1 · Baseline becomes the first data point for the Step 3 chart (st.line_chart over baseline + weekly rest_assessments; no interpretive copy v1) | roadmap | Next change |
| v4.1 · Translation round pending: 8 strings (3 missing from the Step-1 return + 5 Step-2) | pipeline | Procurement count discipline |

| v4.1 · Step 1 + Step 2 fully localized (8/8 strings returned — procurement discipline clean): adjust/habit notes and the five rest strings (why-recheck explainer, baseline block, per-input captions) now render from ui_strings (162 keys, count pinned); constants retained as EN contract values (drift pins) | ui_strings ×3, summary_view v4, rest_view v6 | Predicted 324 |
| v4.1 · Step 3 (chart) design settled pending one team decision — zero-value semantics (recommended: plot as-entered; the 0-means-skipped fix, if wanted, belongs in the form defaults with sign-off, not a chart filter); series labels REUSE rest.* strings (zero new line copy); no interpretive text v1; 2 tiny x-label strings provisional EN; new read query get_rest_assessment_history | pending | #10 un-park |
| v4.1 · Phase 9, Step 3 shipped (parked #10 un-parked; team decision (a) — plot as-entered, pinned by test): st.line_chart under the baseline block; baseline = Day-1 point for the stance series ONLY (15s sit-to-stand ≠ 30s baseline — no Day-1 point, the Step 2 discipline); series names reuse rest.* labels (zero new line copy — trilingual day one); entered 0 plots as 0, DB NULL plots as a gap (the DB distinguishes — the chart never filters); no interpretive copy v1; x-labels (Day 1 / Week {n}) provisional EN, next round; new read query get_rest_assessment_history (rollback hygiene); pandas = transitive dep of pinned streamlit (no new runtime dep); chart visuals = permanent manual-QA line (AppTest blind spot) | rest_view, queries, tests | Predicted 327 |
| v4.1 · Phase 9 COMPLETE (steps 1–3, all localized except the two chart x-labels): summary debrief + baseline storage/display + weekly progression chart — the retention & motivation set | — | §from beta feedback to chart in three green changes |
| v4.1 · Next (tiny) translation round: 2 strings — CHART_DAY1 "Day 1" / CHART_WEEK "Week {n}" | pipeline | The last untranslated strings in the app |

| v4.1 · Phase 9 correction (test_progress_chart_data caught a latent v4.1 defect): rest_assessments SLS columns INTEGER→REAL + int() truncation removed from the submit path — both baseline and weekly forms collect 0.5-step values (#7 grid); the silent half-second loss was harmless until the chart compared baseline (JSONB-exact) with weekly (truncated) values. Decision (a) applied at the source: store as-entered. The FIRST type-widening migration: plain splitter-safe ALTERs (no DO-block guards — the statement splitter forbids internal semicolons); existing int values convert losslessly; same-type re-runs are no-ops; one-time tiny-table rewrite lands on MAIN at next deploy | db/schema, rest_view, tests | 327 collected = predicted; the test was the pin |
| v4.1 · Pending: the 2-string chart x-label round (Day 1 / Week {n}) — the last untranslated strings; chart visual = the new permanent manual-QA line | pipeline, QA | — |

| v4.1 · Phase 9 Step 3 v8 presentation redesign (user request — visibility): metric-row headline (st.metric deltas: stance vs Day 1, sit-to-stand vs previous review; 26px values per existing CSS; labels reused — zero new copy) + ONE single-series bar chart per measure (st.bar_chart STACKS multi-series — meaningless for independent measures in mixed units; bars start at zero — honest magnitudes; fixes the v7 line chart's mixed-unit axis); x-labels tr-driven — the final 2-string round landed (164 keys, count pinned): the app is now 100% localized | rest_view, ui_strings ×3, tests | Predicted 327 |
| v4.1 · Manual QA line updated: metric row + per-measure bars visual check (AppTest blind to chart/metric visuals) | QA | The images/audio precedent class |
| v4.1 · Phase 9 COMPLETE: summary debrief + baseline storage/display + progress presentation, fully localized; remaining ops: deploy per the standing procedure (beta-test pre-deploy suite → push → smoke + idle-gap drill + the chart's manual line) | roadmap | — |

| v4.1 · Phase 10 (user-approved): wizard BACK NAVIGATION (flow-only, sign-off recorded — safety contract, scoring, and clinical copy untouched): Back above each step's form (st.form constraint); Steps 1–3 → previous; Step 4 → derived target (Step 0 fast-path recovery / Step 3 normal); values persist by widget key; _previous_step pure + unit-tested; manual-QA line added | onboarding_wizard v6, tests, STATE.md | Predicted 328 |
| v4.1 · Chart settings prototype delivered (throwaway, prototype-first rule applied to the new Altair surface at the user's request): variants A–G across domain multiplier / markers / norm rule / sts line-vs-bar / font pass; verdict selects the production settings, then deletion | prototype_chart.py | Chart v3 next |
| v4.1 · Translation: 2 strings landed (Back / 上一步; Reference level / 參考水平 — single zh form applied to both locales); count 166 | ui_strings ×3 | The app remains 100% localized |

| v4.1 · Phase 10, chart v3 shipped per the prototype verdict (G + top legend; F = sit-to-stand line): ONE Altair stance line chart (L+R, markers, 3.5 stroke, y-domain [0, 1.5×max(data, norm)] zero-floor, top legend, 16px labels, the norm reference rule — value read from the rubrics, data-driven; label = rest.reference_level + value, matching the approved prototype visual); the x-axis is the FULL journey (Day 1 + Weeks 0–4 with NULL placeholders — the formative horizon); sit-to-stand as a single-series line chart, weeks-only, label above, rendered once a value exists (no norm — the 30s rubric is not comparable to the 15s weekly test). altair = transitive dep (prototype-verified: every parameter EXCEPT Legend(orient="top") — the verdict's tweak, first manual-QA check). Prototype deleted per the standing rule | rest_view v9, tests | Predicted 329 |
| v4.1 · Manual-QA line updated (chart visuals — the AppTest blind-spot precedent class) | QA | — |
| v4.1 · Phase 10 COMPLETE: wizard back navigation + the verdict-G progression chart, all localized | — | Beta feedback → fix → chart in four green changes |

| v4.1 · Count discipline applied to the assistant's own prediction: "329 predicted" was a prose miscount (the delivered test file defines 3 tests — domain assertions folded in); 328 collected is authoritative; recount confirmed, nothing dropped | process record | The mismatch triggered the recount, as designed |
| v4.1 · Phase 10 v2 (user feedback, signed off via thread): Back button compact + BELOW the primary action (full-width top placement read as out of place; 48px floor via CSS); confirmation-page readability — red flags display localized LABELS never codes (the raw-code display was a genuine defect), per-measure score lines with plain-language descriptions, score legend, intro line (teaches Back), level note (states existing autoregulation); wizard.metrics goes dormant (translation retained) | onboarding_wizard v7, tests | Predicted 329 |
| v4.1 · Provisional EN (7 strings) pending live review + the next translation round | pipeline | The wizard copy joins the queue |

| v4.1 · Phase 10 v3 — the widget-state/option-list class fixed at all three sites: the sex selectbox key is LOCALE-TAGGED (a live auth-page locale switch left the stale EN option displayed — beta-reported); the wizard's SARC-F and preference selectbox keys locale-tagged proactively (latent: re-assessment after a hub locale switch — key-only, the Phase 5 non-clinical precedent); pinned by test_sex_default_follows_locale | auth_view, sarc_f_assessment, wizard, tests | The picker-v3/RPE class, third appearance |
| v4.1 · Chart axis titles fixed (user-reported): Altair was rendering the raw field names ("x" / "value") — a defect inherited from the prototype; x untitled, y carries the localized units (rest.y_seconds 秒 / rest.y_count 次數 — user-provided strings; 175 keys); the stance layers share encoding objects so the axis resolution is consistent by construction | rest_view v10, ui_strings ×3 | Predicted 330 |